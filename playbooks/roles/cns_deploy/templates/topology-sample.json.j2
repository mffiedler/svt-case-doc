{
    "clusters": [
        {
            "nodes": [
                {
                    "node": {
                        "hostnames": {
                            "manage": [
                                "{{ node_0 }}"
                            ],
                            "storage": [
                                "{{ hostvars[groups['glusterfs'][0]]['ansible_default_ipv4']['address'] }}"
                            ]
                        },
                        "zone": 1
                    },
                    "devices": {{glusterfs_devices}}
                },
                {
                    "node": {
                        "hostnames": {
                            "manage": [
                                "{{ node_1 }}"
                            ],
                            "storage": [
                                "{{ hostvars[groups['glusterfs'][1]]['ansible_default_ipv4']['address'] }}"
                            ]
                        },
                        "zone": 2
                    },
                    "devices": {{glusterfs_devices}}
                },
                {
                    "node": {
                        "hostnames": {
                            "manage": [
                                "{{ node_2 }}"
                            ],
                            "storage": [
                                "{{ hostvars[groups['glusterfs'][2]]['ansible_default_ipv4']['address'] }}"
                            ]
                        },
                        "zone": 1
                    },
                    "devices": {{glusterfs_devices}}
                }
            ]
        }
    ]
}
